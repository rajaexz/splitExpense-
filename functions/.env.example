# Copy this file to .env and paste your key (do not commit .env).
# Get a key: https://aistudio.google.com/apikey
# After exposing a key in chat/git, revoke it and create a new one.

GEMINI_API_KEY=
# Optional override (default in code is gemini-flash-latest, same family as REST v1beta models/gemini-flash-latest)
# GEMINI_MODEL=gemini-2.0-flash
